# Full Pipeline Requirements
# Tested on Python 3.10+ with CUDA 12.1

# Core ML/DL stack
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1

# Unsloth (for efficient LoRA training)
# Note: Install unsloth with the following command for best compatibility:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# Or use the stable version:
unsloth>=2026.1
unsloth-zoo>=2026.1

# Transformers ecosystem
transformers>=4.48.0
trl>=0.13.0
peft>=0.14.0
accelerate>=1.3.0
datasets>=3.2.0
tokenizers>=0.21.0
huggingface-hub>=0.28.0

# Quantization support
bitsandbytes>=0.45.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# HTTP client (for vLLM API calls)
requests>=2.31.0

# Utilities
tqdm>=4.65.0
